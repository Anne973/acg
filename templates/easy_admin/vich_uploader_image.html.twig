

{%for image in value%}


        <a href="#" class="easyadmin-thumbnail" data-featherlight="#easyadmin-lightbox-{{ image.id }}" data-featherlight-close-on-click="anywhere">

            <img src="{{ asset(vich_uploader_asset(image, 'imageFile')) }}" >
        </a>

        <div id="easyadmin-lightbox-{{ image.id }}" class="easyadmin-lightbox">
            <img src="{{ asset(vich_uploader_asset(image, 'imageFile')) }}">    </div>

{% endfor %}
